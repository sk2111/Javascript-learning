"use strict";
a = 10; //bad not allowed 
var a = 10; //good allowed 
console.log(a);


/* Learning Tips
1) use strict allows the broswer to work with modern way javascript
2) one such example is above where we cannot above to assgin value to variable without declaration
3) By default classes and modules comes with use strict directive
4) use strict should be at the top of the file or top of function(only function level)
*/